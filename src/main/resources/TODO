# Hardcoded DB Creds
#Oauth
#Aop
#Sto test controller to retry tou controller paizei, na mpei kai sta kanonika controllers twn transaction, dokimasw me retryable

docker-compose up --build
http://localhost:8080/swagger-ui/index.html